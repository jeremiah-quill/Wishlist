<button id="logout">logout for testing</button>

<div>User Dashboard</div>

<div>Username: {{username}}</div>

<div>Email: {{email}}</div>

{{#each gifts as |gift|}}

  <div>Gift: {{gift.gift_name}}</div>

{{/each}}

{{#each groups as |group|}}

  <div>Member of group: {{group.event_name}}</div>

{{/each}}