<button type="button" class="btn btn-success">Group Dashboard</button>
<button id="logout" type="button" class="btn btn-primary btn-md">Log Out</button>
<h1>Welcome!</h1>

<div class="user-dashboard-container">
  <div class="wishlist-container">
    <div class="wishlist-card card">
      <!-- <span class="border border-success"> -->
      <div class="card-header">
        Your Wishlist
      </div>
      <div class="card-body">
        <!-- Button trigger modal -->
        <button
          type="button"
          class="btn btn-primary"
          data-bs-toggle="modal"
          data-bs-target="#giftModal"
        >
          Add Gift
        </button>
        <div class="card-body">
          <ul class="gifts">
            {{#each gifts as |gift|}}
              <div class="gift-item" data-gift-id="{{gift.id}}">
                <li class="gift">{{gift.gift_name}}</li>
                <div class="gift-button-container">
                  <button
                    id="edit-button"
                    data-bs-toggle="modal"
                    data-bs-target="#editGiftModal"
                    class="edit-button gift-button btn btn-success btn-lg"
                  >Edit</button>
                  <button
                    class="gift-button delete-button btn btn-success btn-lg"
                  >Delete</button>
                </div>
              </div>
            {{/each}}
          </ul>

        </div>
      </div>
    </div>
  </div>

  <div class="group-list-container col-6 col-sm-12">
    <div class="card text-center">
      <div class="card-header">
        Your Groups
      </div>
      <div class="card-body">

        <div class="group-buttons">
          <a href="/create-group" class="btn btn-primary">Create a Group</a>
          <a href="/join-group" class="btn btn-primary">Join a Group</a>
        </div>

        <div class="card-body">
          <ul class="group-list">
            {{#each groups as |group|}}
              <li class="group-item"><a
                  href="/group/{{group.id}}"
                >{{group.event_name}}</a></li>
            {{/each}}
          </ul>
        </div>

      </div>
    </div>
  </div>
</div>

{{! ************************************************* }}
<!-- Add gift modal -->
<div
  class="modal fade"
  id="giftModal"
  tabindex="-1"
  aria-labelledby="exampleModalLabel"
  aria-hidden="true"
>
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Add gift details</h5>
        <button
          type="button"
          class="btn-close"
          data-bs-dismiss="modal"
          aria-label="Close"
        ></button>
      </div>
      <div class="modal-body">
        <form class="add-gift-form">
          <div class="mb-3">
            <label for="exampleInputEmail1" class="form-label">Gift Name</label>
            <input
              type="text"
              class="form-control"
              id="add-gift-name"
              aria-describedby="emailHelp"
              placeholder="Required"
            />
          </div>
          <div class="mb-3">
            <label for="exampleInputPassword1" class="form-label">Gift Price</label>
            <input
              type="number"
              class="form-control"
              id="add-gift-price"
              placeholder="Optional"
            />
          </div>
          <div class="mb-3">
            <label for="exampleInputPassword1" class="form-label">Gift Link</label>
            <input
              type="text"
              class="form-control"
              id="add-gift-link"
              placeholder="Optional"
            />
          </div>
          <button
            id="add-gift-button"
            type="submit"
            class="btn btn-primary"
          >Submit</button>
        </form>
      </div>
    </div>
  </div>
</div>

<!-- Edit gift modal -->
<div
  class="modal fade"
  id="editGiftModal"
  tabindex="-1"
  aria-labelledby="exampleModalLabel"
  aria-hidden="true"
>
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Edit gift details</h5>
        <button
          type="button"
          class="btn-close"
          data-bs-dismiss="modal"
          aria-label="Close"
        ></button>
      </div>
      <div class="modal-body">
        <form id="edit-gift-form" data-gift-id="">
          <div class="mb-3">
            <label for="exampleInputEmail1" class="form-label">Gift Name</label>
            <input
              type="text"
              class="form-control"
              id="edit-gift-name"
              aria-describedby="emailHelp"
              placeholder="Required"
            />
          </div>
          <div class="mb-3">
            <label for="exampleInputPassword1" class="form-label">Gift Price</label>
            <input
              type="number"
              class="form-control"
              id="edit-gift-price"
              placeholder="Optional"
            />
          </div>
          <div class="mb-3">
            <label for="exampleInputPassword1" class="form-label">Gift Link</label>
            <input
              type="text"
              class="form-control"
              id="edit-gift-link"
              placeholder="Optional"
            />
          </div>
          <button type="submit" class="btn btn-primary">Submit</button>
        </form>

      </div>
    </div>
  </div>
</div>

<script src="./js/userDashboard.js"></script>